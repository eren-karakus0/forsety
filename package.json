{
  "name": "forsety",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "clean": "turbo run clean",
    "seed": "tsx tooling/scripts/seed-demo.ts",
    "seed:local": "tsx tooling/scripts/seed-demo.ts --local-only",
    "check:shelby": "tsx tooling/scripts/check-shelby.ts",
    "changeset": "changeset",
    "backfill:canonical": "tsx tooling/scripts/backfill-canonical.ts"
  },
  "pnpm": {
    "overrides": {
      "tar": ">=7.5.11",
      "flatted": ">=3.4.0",
      "hono": ">=4.12.7",
      "esbuild": ">=0.25.0"
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@forsety/db": "workspace:*",
    "@vitest/coverage-v8": "^2.1.9",
    "canonicalize": "^2.1.0",
    "drizzle-orm": "^0.38.0",
    "tsx": "^4.19.0",
    "turbo": "^2.5.0",
    "typescript": "^5.7.0"
  }
}
